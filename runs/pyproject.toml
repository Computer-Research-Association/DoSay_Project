[project]
name = "AppleGameRuns"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    # 필요하면 메인 프로젝트를 의존성으로 명시 가능
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["agents*"]